server:
  port: 8080

# Ollama 本地模型配置
spring:
  application:
    name: springai
  config:
    import:
      - optional:nacos:ark.yaml?group=DEFAULT_GROUP      # 火山引擎配置
      - optional:nacos:redis.yaml?group=DEFAULT_GROUP    # Redis 配置
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        group: DEFAULT_GROUP
        file-extension: yaml
        refresh-enabled: true
  # PostgreSQL 数据源配置
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/springai
    username: postgres
    password: root
  # JPA 配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  ai:
    ollama:
      base-url: http://localhost:11434
      model: llama3
      timeout: 60000
    # pgvector 向量存储配置（如果需要）
    vectorstore:
      pgvector:
        index-type: HNSW
        distance-type: COSINE_DISTANCE
        dimensions: 1536

# MyBatis-Plus 配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: assign_id
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-locations: classpath*:/mapper/**/*.xml

# 会话配置（不在 spring 下，是自定义配置）
session:
  redis:
    key-prefix: "chat:session:"
    ttl: 3600  # 会话过期时间（秒）

# 提示词模板配置
prompt:
  redis:
    key-prefix: "prompt:template:"
    list-key: "prompt:list"
